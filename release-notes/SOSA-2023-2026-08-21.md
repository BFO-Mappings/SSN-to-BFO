# Release identity

Release identifier: `2026-08-21`. Intended Git tag: `v2026-08-21`.

The immutable SOSA source identity for this release track is `sosa-2023-af425a0454ec00512a5ebfa2873fe35a077f5fda`. The governed manifest records the final exact source commit used to construct the release. These notes describe the intended formal release; committing them does not itself create a Git tag, GitHub release, upload, or persistent-IRI deployment.

# Included products

The formal SOSA-2023 package contains exactly three ontology products:

- `sosa-2023-af425a0454ec00512a5ebfa2873fe35a077f5fda/sosa-integrated.ttl` - SOSA-2023 Integrated Mapping. Directly asserts all 55 active governed mapping axioms.
- `sosa-2023-af425a0454ec00512a5ebfa2873fe35a077f5fda/sosa-bfo-mapping.ttl` - SOSA-2023 Strict BFO Mapping. Directly asserts the 24 governed BFO-bearing mapping axioms.
- `sosa-2023-af425a0454ec00512a5ebfa2873fe35a077f5fda/sosa-cco-extension.ttl` - SOSA-2023 CCO Extension. Directly asserts the 30 CCO-bearing axioms and one mixed BFO/CCO axiom and imports the same-release Strict BFO Mapping.

The package also contains its canonical catalog, manifest, checksum inventory, release notes, project license, governed COMS workbook, source-version authority, publication metadata, release-scope policy, and product-role policy.

# Product selection guidance

Use the Integrated product when the complete governed SOSA-2023 mapping is needed in one ontology.

Use the Strict BFO Mapping when only the governed BFO-bearing mapping layer is required.

Use the CCO Extension when the governed CCO-bearing and mixed BFO/CCO mappings are required. It imports the same-release Strict BFO Mapping.

The formal product-role policy does not materialize separate Alignment Core or BFO Projection products for this release track.

# Governed axiom and closure summary

The governed workbook contains 119 rows with 119 unique RowIDs. All governed mapping decisions are final:

| Mapping status | Count |
|---|---:|
| Active | 55 |
| No direct mapping | 64 |
| Deferred | 0 |
| Unreviewed | 0 |

The 64 no-direct-mapping rows are reviewed semantic decisions and intentionally produce no direct ontology axiom.

Direct governed axiom counts and fixed formal HermiT reasoning-closure triple counts are separate measures:

| Product | Direct governed axioms | Fixed formal reasoning-closure triples |
|---|---:|---:|
| Integrated | 55 | 15,243 |
| Strict BFO Mapping | 24 | 15,120 |
| CCO Extension | 31 | 15,254 |

The Integrated product directly contains the complete 55-axiom authoritative mapping set. The modular BFO and CCO products partition that active mapping content according to the governed product-role policy.

# Import graph

The Integrated product retains the governed external SOSA source-ontology imports and the merged CCO/BFO target dependency required by the mapping.

The Strict BFO Mapping has no same-release project import.

The CCO Extension imports the same-release immutable version IRI of the Strict BFO Mapping.

Formal package validation resolves same-release project imports entirely inside the package. External ontology dependencies are separately identified and hash-governed by the manifest and are not redistributed as package members.

# BFO projection notice

No BFO Projection product is materialized for the SOSA-2023 formal track. No approved weakened but sound BFO consequence currently provides a distinct projection-layer consumer function.

This omission is governed by the product-role policy and does not alter the 24 active BFO-bearing axioms in the Strict BFO Mapping.

# Validation summary

The final governed mapping state has passed the complete repository validation suite and hosted CI.

Validation includes governed RowID and MappingStatus checks, deterministic maintained-product reconstruction, exact product hashes and triple counts, source-version authority, product-role and release-scope checks, formal publication rendering, canonical manifest construction, deterministic package and raw-USTAR archive validation, checksum-sidecar validation, ROBOT reconstruction, instance-data smoke tests, ELK entailment tests, Python compilation, Git whitespace validation, and local HermiT consistency checks with zero named unsatisfiable classes.

Formal publication remains contingent on the final clean-source release rehearsal against the exact committed source revision containing these notes. That rehearsal independently builds two detached candidates and requires byte-identical complete packages, manifests, archives, and checksum sidecars.

# Known limitations

- Sixty-four governed mapping rows have final `no_direct_mapping` decisions. These are reviewed semantic decisions rather than unresolved mappings and intentionally contribute no direct axiom.
- No BFO Projection product is materialized because no approved sound weakened projection currently provides a distinct consumer function.
- External SOSA and CCO/BFO ontology dependencies are governed by exact identities and hashes but are not redistributed inside the formal package.
- Package-local catalog resolution covers the formal project products; the release tooling does not itself deploy public HTTP redirects, content negotiation, or other persistent-IRI hosting infrastructure.

# Deferred functionality

No governed SOSA-2023 mapping decision remains deferred.

Future mappings, new transformation capabilities, a distinct BFO projection layer, or datatype-property mapping infrastructure require separate semantic justification and governance before they can become release content.

Public persistent-IRI hosting and related HTTP deployment are separate operational actions and are not performed by the package, archive, or rehearsal tooling.

# License scope

Governed project release files are distributed under the repository's CC0 dedication.

Third-party SOSA, BFO, CCO, and other ontology dependencies retain their own notices and terms. The project CC0 dedication does not relicense external ontology content, and validation-only external dependencies are not redistributed as formal package members.

# Dependencies

Deterministic formal validation uses the pinned SOSA-2023 source ontology, SOSA Systems ontology, SOSA Sampling ontology, and merged CCO/BFO target dependency governed by the repository.

The canonical release manifest records the exact dependency identities, hashes, byte sizes, and validation roles. These dependencies are validation and ontology-import inputs rather than redistributed package members.

# Reproduction

Run the final clean-source verification from the exact committed release source revision:

```sh
SOURCE_COMMIT="$(git rev-parse HEAD)"

PYTHONDONTWRITEBYTECODE=1 python -B tools/sosa_2023_rehearse_release.py verify \
  --release-id 2026-08-21 \
  --release-date 2026-08-21 \
  --git-tag v2026-08-21 \
  --source-commit "${SOURCE_COMMIT}" \
  --notes release-notes/SOSA-2023-2026-08-21.md
```

To retain the independently reproduced release package, canonical archive, and checksum sidecar after the two-candidate proof succeeds, use an absent external output directory:

```sh
SOURCE_COMMIT="$(git rev-parse HEAD)"
WORK_ROOT="$(mktemp -d)"
OUTPUT_DIR="${WORK_ROOT}/sosa-2023-release-output"
trap 'rm -rf "${WORK_ROOT}"' EXIT

PYTHONDONTWRITEBYTECODE=1 python -B tools/sosa_2023_rehearse_release.py build \
  --release-id 2026-08-21 \
  --release-date 2026-08-21 \
  --git-tag v2026-08-21 \
  --source-commit "${SOURCE_COMMIT}" \
  --notes release-notes/SOSA-2023-2026-08-21.md \
  --output-dir "${OUTPUT_DIR}"
```

The rehearsal does not create the intended Git tag, create a GitHub release, upload release assets, or deploy persistent IRIs. Those remain separate publication actions after successful rehearsal.
